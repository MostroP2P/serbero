# Phase 3 Escalation Policy (STUB — not live content)

Structural stub. The real escalation policy MUST enumerate every
trigger in `spec.md` FR-111 and the `EscalationTrigger` enum in
`src/models/mediation.rs` once that file is complete.

## Scope

- Defines when a session MUST transition to `escalation_recommended`
  rather than continue guided mediation.
- Defines what evidence the Phase 4 handoff package MUST contain
  for each trigger.

## Rules / Guidance

- TODO: enumerate triggers: `ConflictingClaims`, `FraudIndicator`,
  `LowConfidence`, `PartyUnresponsive`, `RoundLimit`,
  `ReasoningUnavailable`, `AuthorizationLost`,
  `AuthorityBoundaryAttempt`, `MediationTimeout`,
  `PolicyBundleMissing`.
- TODO: state evidence requirements per trigger.

## Examples

- TODO: representative sessions per trigger.
