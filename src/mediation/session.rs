//! Mediation session lifecycle (US1/US2 — not yet implemented).
//!
//! Will own `open_session`, `ingest_inbound`, `check_round_limit`,
//! and the state-machine transition enforcement defined in
//! `data-model.md`.

// TODO(US1): open_session(ctx, dispute_id).
// TODO(US2): ingest_inbound(ctx, session, envelope).
// TODO(US4): check_round_limit(session, max_rounds).
